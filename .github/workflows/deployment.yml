name: Deploy Factory Tycoon Backend

on:
  push:
    branches:
      - main
      - deploy
    paths:
      - 'src/**'
      - 'build.gradle'
      - 'Dockerfile'
      - '.github/workflows/deployment.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_YJ }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_YJ }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image to ECR
        id: build-image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/sf-backend:latest
            ${{ steps.login-ecr.outputs.registry }}/sf-backend:${{ github.sha }}
          cache-from: type=registry,ref=${{ steps.login-ecr.outputs.registry }}/sf-backend:latest
          cache-to: type=inline

      - name: Image digest
        run: |
          echo "Image pushed to ECR:"
          echo "${{ steps.login-ecr.outputs.registry }}/sf-backend:latest"
          echo "${{ steps.login-ecr.outputs.registry }}/sf-backend:${{ github.sha }}"

      - name: Update Helm Chart Image Tag
        run: |
          git clone https://x-access-token:${{ secrets.GH_PAT }}@github.com/lgcns5team/factory-tycoon-k8s.git
          cd factory-tycoon-k8s/helm/factory-tycoon-backend
          sed -i "s|tag:.*|tag: \"${{ github.sha }}\"|g" values-prod.yaml
          cd ../..
          git config --global user.email "actions@github.com"
          git config --global user.name "github-actions"
          git add .
          git commit -m "Update backend image tag to ${{ github.sha }}"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/lgcns5team/factory-tycoon-k8s.git